# Optional: bake an OpenRouteService key into local dev builds.
# The app also lets you paste the key in Settings (stored only in your browser).
# Note: anything in a static site is visible to whoever opens it.
VITE_ORS_KEY=
